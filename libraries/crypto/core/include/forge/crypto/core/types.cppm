module;

#include <cstddef>
#include <cstdint>
#include <vector>

export module forge.crypto.core.types;

export namespace forge::crypto::core {

using bytes = std::vector<std::uint8_t>;

} // namespace forge::crypto

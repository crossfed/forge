add_library(forge_contract_attributes STATIC registry.cpp)
forge_target_modules_at(forge_contract_attributes contract/attributes)

set_target_properties(forge_contract_attributes PROPERTIES POSITION_INDEPENDENT_CODE ON)

target_include_directories(forge_contract_attributes PUBLIC ${FORGE_PUBLIC_INCLUDE_DIRS})
target_include_directories(forge_contract_attributes SYSTEM PRIVATE ${CLANG_INCLUDE_DIRS} ${LLVM_INCLUDE_DIRS})
target_compile_definitions(forge_contract_attributes PRIVATE ${LLVM_DEFINITIONS})
target_link_libraries(forge_contract_attributes PRIVATE clang-cpp LLVM)

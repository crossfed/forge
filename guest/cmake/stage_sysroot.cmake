if(NOT DEFINED SOURCE OR NOT IS_DIRECTORY "${SOURCE}")
   message(FATAL_ERROR "SOURCE must name an existing sysroot")
endif()
if(NOT DEFINED DESTINATION OR DESTINATION STREQUAL "")
   message(FATAL_ERROR "DESTINATION is required")
endif()
if(NOT DEFINED STAMP OR STAMP STREQUAL "")
   message(FATAL_ERROR "STAMP is required")
endif()

file(REMOVE_RECURSE "${DESTINATION}")
file(MAKE_DIRECTORY "${DESTINATION}")
file(COPY "${SOURCE}/" DESTINATION "${DESTINATION}")
file(TOUCH "${STAMP}")

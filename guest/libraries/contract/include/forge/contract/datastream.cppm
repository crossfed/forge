export module forge.contract.datastream;

export import forge.raw.codec;
export import forge.raw.stream;

export namespace forge::contract {

using ::forge::datastream;
using ::forge::raw::pack;
using ::forge::raw::pack_size;
using ::forge::raw::unpack;
using ::forge::raw::unpack_exact;

} // namespace forge::contract

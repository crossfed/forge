#pragma once

#include <cstdint>
#include <limits>

namespace product::chain::details {

inline constexpr auto maximum_value = std::numeric_limits<std::uint64_t>::max();

} // namespace product::chain::details
